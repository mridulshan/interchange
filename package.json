{
  "name": "interchange",
  "version": "0.1.0",
  "description": "A cross-repo map of what you shipped, in what order, and what each thing holds up.",
  "type": "module",
  "bin": { "interchange": "./dist/cli.js" },
  "files": ["dist", "README.md"],
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "serve": "npm run build && node dist/cli.js serve",
    "check": "npm run build && node dist/cli.js check",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
